<?xml version="1.0" encoding="utf-8"?>
<rotate
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromDegrees="0"
    android:pivotX="50%"
    android:pivotY="50%"
    android:toDegrees="360">
    <shape
        android:innerRadius="12dp"
        android:shape="ring"
        android:thickness="3dp"
        android:useLevel="false">
        <!--useLevel的作用就是去掉动画，如果设置为false则为去掉动画，默认为true
            需要注意的是，如果是将useLevel设置为false后，那么Animation就没有动画了
            如果需要动画的就需要自己来定制，方式就是在shape的外面加上一个rotate标签
            比如上面，就是设置成了0——360旋转，旋转点在中心
        -->

        <gradient
            android:centerColor="#3f00"
            android:endColor="#f00"
            android:startColor="#fff"/>
    </shape>
</rotate>